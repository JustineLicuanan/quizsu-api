import { Request, Response } from 'express';

// Create a quiz
export const createPOST = (req: Request, res: Response) => {};

// Get a quiz
export const getQuizGET = (req: Request, res: Response) => {};
